# Production environment overrides
# CORS is handled by API Gateway - no Lambda CORS configuration needed

# Production has stricter image size limits (commons service handles processing)
MAX_IMAGE_SIZE=3145728